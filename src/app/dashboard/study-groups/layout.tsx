export const metadata = {
  title: "UniShare | Study Groups",
  description: "Find and join study groups at your university",
};

export default function StudyGroupsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
