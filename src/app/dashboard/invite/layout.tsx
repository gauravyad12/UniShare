export const metadata = {
  title: "UniShare | Invite Friends",
  description: "Invite your classmates to join UniShare",
};

export default function InviteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
